{
  "schemaVersion": 1,
  "project": "agentskit-code-review-quality-matrix",
  "root": "..",
  "profile": "custom",
  "exemptions": [
    "This repository is a CLI/library; endpoint, database, MCP, and UI product surfaces are out of scope for this fix."
  ],
  "contract": {
    "intent": "Implement and validate an automatic quality matrix for real Orca PR-review cycles, preserving deterministic batch safety and the Changesets-based, tokenless npm release path for @agentskit/code-review.",
    "outcomes": [
      { "id": "batch-manifest", "statement": "The CLI exposes a deterministic reviewable-file manifest, batch plan, and private SHA/policy-bound manifest without model execution.", "checks": ["batch-regression", "repo-check"] },
      { "id": "batch-artifact", "statement": "A partial batch writes a private artifact bound to the exact PR SHA, policy fingerprint, index, and file manifest; it cannot post to GitHub.", "checks": ["batch-regression", "repo-check"] },
      { "id": "batch-consolidation", "statement": "Only every matching, successful batch artifact can consolidate to a publishable review; stale, duplicate, incomplete, or mismatched artifacts fail closed.", "checks": ["batch-regression", "repo-check"] },
      { "id": "reviewable-formats", "statement": "HTML, CSS, Markdown, and MDX changes are reviewable rather than silently left outside coverage.", "checks": ["batch-regression", "repo-check"] },
      { "id": "inline-guidance", "statement": "GitHub inline findings explain why a correction is needed, the required change, an acceptance check, and confidence.", "checks": ["batch-regression", "repo-check"] },
      { "id": "action-budget", "statement": "The GitHub Action defaults stay within the provider-call ceiling and expose max-calls and max-findings-per-file controls.", "checks": ["regression", "repo-check"] },
      { "id": "provider-auth", "statement": "CI gives local CLI providers an explicit safe authentication path and fails early when Codex cannot use the selected isolated mode.", "checks": ["repo-check"] },
      { "id": "fingerprint", "statement": "Review idempotency fingerprints use the installed package version rather than a stale hardcoded version.", "checks": ["repo-check"] },
      { "id": "comment-pagination", "statement": "Review state and summary updates inspect bounded comment history and fail closed before posting when history is truncated.", "checks": ["repo-check"] },
      { "id": "bounded-http", "statement": "GitHub API and reporter responses have shared size limits, deadlines, and transient GET retry behavior.", "checks": ["repo-check"] },
      { "id": "integration-regressions", "statement": "Integration coverage exercises Action defaults, authentication routing, pagination overflow, bounded response bodies, and existing provider failures.", "checks": ["repo-check"] },
      { "id": "repo-gates", "statement": "The package typechecks, tests, documentation gates, and packs successfully.", "checks": ["repo-check", "package"] },
      { "id": "auth-fail-fast", "statement": "Terminal provider authentication failures stop additional provider calls while preserving fail-closed review semantics.", "checks": ["regression", "repo-check"] },
      { "id": "provider-health", "statement": "Normal Codex runs execute one bounded provider smoke check before fan-out and fail early on authentication or timeout errors.", "checks": ["regression", "repo-check"] },
      { "id": "global-deadline", "statement": "A review-wide deadline propagates cancellation to active adapters and prevents queued provider calls from starting after expiry.", "checks": ["regression", "repo-check"] },
      { "id": "fast-profile", "statement": "The explicit fast profile batches correctness, security, and tests with one vote and no retry, while preserving required-lens fail-closed semantics.", "checks": ["regression", "repo-check"] },
      { "id": "circuit-breaker", "statement": "Repeated transient provider failures and terminal provider failures stop unnecessary fan-out and expose skipped-call evidence.", "checks": ["repo-check"] },
      { "id": "execution-evidence", "statement": "Completed reports expose profile, provider calls, failures, skips, elapsed time, deadline status, and circuit state.", "checks": ["regression", "repo-check"] },
      { "id": "continuous-improvement-benchmark", "statement": "A deterministic, credential-free benchmark proves clean approval, required-lens failure, and deadline fail-closed behavior with machine-readable evidence.", "checks": ["cycle-benchmark", "repo-check"] },
      { "id": "release-version", "statement": "Package metadata, lockfile, public examples, changelog, npm publication, and immutable release tag agree on the current Changesets version.", "checks": ["repo-check", "docs-gate", "package"] },
      { "id": "changesets-release", "statement": "Changesets creates a reviewable version pull request on main, and only its bot-authored merge can trigger a validated OIDC npm publication and immutable GitHub release without a long-lived npm token.", "checks": ["release-workflow", "repo-check", "package"] },
      { "id": "self-hosted-memory", "statement": "The public memory configuration creates a bounded repository-local AgentsKit ChatMemory, persists messages, applies retention, and fails closed on malformed records.", "checks": ["repo-check", "package"] },
      { "id": "memory-learning-effectiveness", "statement": "Only explicitly approved bounded memory rules enter review context, and a live A/B canary proves detection lift without precision or token regression before memory-learning can pass.", "checks": ["memory-learning", "repo-check", "package"] },
      { "id": "comment-policy", "statement": "The public comment configuration reaches GitHub reporters and controls inline output, summary output, language labels, and detail sections without weakening non-approving fallback behavior.", "checks": ["batch-regression", "repo-check"] }
      ,{ "id": "quality-matrix", "statement": "The exported quality matrix scores every approved review-quality area, blocks any unmeasured or sub-three area, and enforces absolute safety, completeness, inline-comment, stale-artifact, and silent-failure gates.", "checks": ["quality-matrix", "repo-check"] }
      ,{ "id": "quality-cli", "statement": "The quality-matrix CLI evaluates a SHA-bound JSON artifact, compares an optional baseline, writes a private report, and returns exit code 2 for BLOCKED results.", "checks": ["quality-matrix", "repo-check", "package"] }
      ,{ "id": "deterministic-cycle", "statement": "One packaged command owns blocker collection, contract lock, replay, canary, crash-safe batch resume, consolidation, quality reports, safe posting and merge gates, and always emits cycle-summary.json.", "checks": ["cycle-runner", "repo-check", "package"] }
    ]
  },
  "surfaces": { "logic": true, "cli": true, "docs": true },
  "checks": [
    { "id": "batch-regression", "category": "logic", "execution": "real", "command": "npm run build && node --test test/batch-coverage.test.mjs test/cli-smoke.test.mjs test/sources.test.mjs test/reporters.test.mjs", "timeoutMs": 240000 },
    { "id": "regression", "category": "logic", "execution": "real", "command": "npm run build && node --test test/cli-smoke.test.mjs", "timeoutMs": 180000 },
    { "id": "cycle-benchmark", "category": "logic", "execution": "real", "command": "npm run benchmark:cycle", "timeoutMs": 60000 },
    { "id": "release-workflow", "category": "cli", "execution": "real", "command": "npm run build && node --test test/release-workflow.test.mjs", "timeoutMs": 60000 },
    { "id": "repo-check", "category": "logic", "execution": "real", "command": "npm run check", "timeoutMs": 300000 },
    { "id": "docs-gate", "category": "docs", "execution": "real", "command": "npm run docs:gate", "timeoutMs": 180000 },
    { "id": "package", "category": "cli", "execution": "real", "command": "npm pack --dry-run", "timeoutMs": 180000 }
    ,{ "id": "quality-matrix", "category": "logic", "execution": "real", "command": "npm run build && node --test --test-concurrency=1 test/quality-matrix.test.mjs test/quality-matrix-cli.test.mjs", "timeoutMs": 60000 }
    ,{ "id": "cycle-runner", "category": "logic", "execution": "real", "command": "npm run build && node --test --test-concurrency=1 test/harness.test.mjs test/review-cycle.test.mjs", "timeoutMs": 180000 }
    ,{ "id": "memory-learning", "category": "logic", "execution": "real", "command": "npm run build && node --test --test-concurrency=1 test/file-memory.test.mjs test/review-learning.test.mjs test/quality-matrix.test.mjs", "timeoutMs": 60000 }
  ],
  "tracking": { "required": false, "reason": "The human authorized a release PR; its URL will be recorded in the release report. Publication remains blocked until the PR is merged and a stable GitHub Release exists." },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
